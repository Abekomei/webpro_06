<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>APEX キャラティア表</title>
</head>
<body>
    <h2>APEX レジェンドティア表</h2>
    <a href="/apex/create">＋ 新規キャラ追加</a>
    <hr>
    <table border="1">
        <tr>
            <th>Tier</th>
            <th>名前</th>
            <th>ロール</th>
            <th>操作</th>
        </tr>
        <% for(let row of data) { %>
        <tr>
            <td style="font-weight:bold; color:red;"><%= row.tier %></td>
            <td><%= row.name %></td>
            <td><%= row.role %></td>
            <td>
                <a href="/apex/detail/<%= row.id %>">詳細</a> | 
                <a href="/apex/edit/<%= row.id %>">編集</a> | 
                <a href="/apex/delete/<%= row.id %>">削除</a>
            </td>
        </tr>
        <% } %>
    </table>
</body>
</html>